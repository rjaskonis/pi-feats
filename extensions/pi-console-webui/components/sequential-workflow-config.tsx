"use client";
import { useEffect, useState } from "react";
import { Save } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { useToast } from "@/components/toast";

type Evaluation = { modelType: "llm" } | { modelType: "system_one"; systemOne: { provider: "typesafe"; model: string; onUncertain: "block" } };
type Props = { profile: string; initial?: { evaluation: Evaluation } };
const api = async <T,>(path: string, init?: RequestInit) => { const response = await fetch(`/api/pi/${path}`, { ...init, headers: { "content-type": "application/json", ...init?.headers } }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error?.message ?? body.error ?? "Request failed"); return body as T; };

export function SequentialWorkflowConfig({ profile, initial }: Props) {
  const { toast } = useToast();
  const [evaluation, setEvaluation] = useState<Evaluation>(initial?.evaluation ?? { modelType: "llm" });
  const [credentialConfigured, setCredentialConfigured] = useState<boolean>();
  const [notice, setNotice] = useState("");
  const load = async () => { try { const [config, status] = await Promise.all([api<{ evaluation: Evaluation }>(`profiles/${profile}/sequential-workflows/config`), api<{ credentialConfigured: boolean }>(`profiles/${profile}/sequential-workflows/status`)]); setEvaluation(config.evaluation); setCredentialConfigured(status.credentialConfigured); setNotice(""); } catch (error) { setNotice((error as Error).message); } };
  useEffect(() => { if (initial) { setEvaluation(initial.evaluation); } void load(); }, [profile, initial]);
  const systemOne = evaluation.modelType === "system_one" ? evaluation.systemOne : { provider: "typesafe" as const, model: "jev-1.13.0", onUncertain: "block" as const };
  const save = async () => { try { const result = await api<{ evaluation: Evaluation }>(`profiles/${profile}/sequential-workflows/config`, { method: "PUT", body: JSON.stringify({ evaluation }) }); setEvaluation(result.evaluation); await load(); toast("Sequential Workflow configuration saved."); } catch (error) { setNotice((error as Error).message); } };
  return <div className="space-y-5"><Card><div className="flex flex-wrap items-start justify-between gap-4"><div><CardTitle>Sequential Workflows</CardTitle><p className="mt-1 text-sm text-zinc-500">Configure how this profile evaluates workflow requirements and criteria.</p></div><Button className="!bg-[#2bbb77] hover:!bg-[#249b63]" title="Save Sequential Workflow configuration" onClick={() => void save()}><Save size={17}/></Button></div>{notice && <p className="mt-4 text-sm text-red-700">{notice}</p>}<div className="mt-5 grid gap-4 md:grid-cols-2"><label className="grid gap-1.5 text-sm font-medium text-zinc-700"><span>Evaluation model type</span><select className="rounded-md border border-zinc-200 bg-white px-3 py-2 font-normal" value={evaluation.modelType} onChange={(event) => setEvaluation(event.target.value === "system_one" ? { modelType: "system_one", systemOne } : { modelType: "llm" })}><option value="llm">LLM</option><option value="system_one">System One Model</option></select></label>{evaluation.modelType === "system_one" && <label className="grid gap-1.5 text-sm font-medium text-zinc-700"><span>System One model/version</span><Input value={systemOne.model} onChange={(event) => setEvaluation({ modelType: "system_one", systemOne: { ...systemOne, model: event.target.value } })} placeholder="jev-1.13.0"/></label>}</div>{evaluation.modelType === "llm" ? <p className="mt-4 rounded-lg bg-sky-50 p-3 text-sm text-sky-900">Uses the active Pi session model for Sequential Workflow decisions.</p> : <div className="mt-4 space-y-3"><div className="grid gap-4 md:grid-cols-2"><label className="grid gap-1.5 text-sm font-medium text-zinc-700"><span>Provider</span><Input value="TypeSafe" disabled/></label><label className="grid gap-1.5 text-sm font-medium text-zinc-700"><span>Uncertain decision policy</span><Input value="Block and require review" disabled/></label></div><p className={`rounded-lg p-3 text-sm ${credentialConfigured ? "bg-emerald-50 text-emerald-900" : "bg-amber-50 text-amber-900"}`}>{credentialConfigured ? "TypeSafe credential is configured for the running environment." : "TypeSafe credential is missing. System One decisions will remain blocked until it is configured."}</p></div>}</Card><Card><CardTitle>Active behavior</CardTitle><dl className="mt-4 grid gap-3 text-sm md:grid-cols-2"><div><dt className="font-medium text-zinc-700">Requirement and template routing</dt><dd className="text-zinc-500">{evaluation.modelType === "system_one" ? "TypeSafe System One" : "Active Pi LLM"}</dd></div><div><dt className="font-medium text-zinc-700">Collect and task criteria</dt><dd className="text-zinc-500">{evaluation.modelType === "system_one" ? "Automatic typed decisions" : "Current agent evaluation flow"}</dd></div><div><dt className="font-medium text-zinc-700">Uncertain or unavailable decisions</dt><dd className="text-zinc-500">Blocked and recorded by the harness.</dd></div><div><dt className="font-medium text-zinc-700">Credentials</dt><dd className="text-zinc-500">Never stored or displayed in settings.</dd></div></dl></Card></div>;
}
